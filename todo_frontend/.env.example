# Environment variables for local development
# API server port for Express (default used by server: 3001)
API_PORT=3001

# Optional: Override API base used by React (defaults to proxy http://localhost:3001)
# REACT_APP_API_BASE=http://localhost:3001
